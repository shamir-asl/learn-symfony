{% extends 'base.html.twig' %}

{% block title %}AgreeblePizza{% endblock %}

{% block body %}
    <h1>AgreeblePizza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agreeble_pizza.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ agreeble_pizza.name }}</td>
            </tr>
            <tr>
                <th>DateDiff</th>
                <td>{{ agreeble_pizza.dateDiff ? agreeble_pizza.dateDiff.format('%y year(s), %m month(s), %d day(s)') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('agreeble_pizza_index') }}">back to list</a>

    <a href="{{ path('agreeble_pizza_edit', {'id': agreeble_pizza.id}) }}">edit</a>

    {{ include('agreeble_pizza/_delete_form.html.twig') }}
{% endblock %}
